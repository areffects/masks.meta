steps:
  - label: 'build mongo'
    command:
      - "docker-compose build -f docker-compose.serivces mongo"

  - wait

  - label: 'start mongo'
    command:
      - "docker-compose up -d -f docker-compose.serivces mongo"
