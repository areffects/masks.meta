steps:
  - label: 'check docker-compose'
    command:
      - "docker-compose --version"

  - wait

  - label: 'start mongo'
    command:
      - "docker-compose -f "docker-compose.services.yml" up -d --build mongo"      
